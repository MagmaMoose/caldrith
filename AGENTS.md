# AGENTS.md

This repo's agent guidance lives in [`CLAUDE.md`](CLAUDE.md). Read that first; it
@-imports the on-demand context in `.claude/` (QUICK_START, ARCHITECTURE_MAP,
COMMON_MISTAKES). The machine-readable module map is `PROJECT_INDEX.json`.
